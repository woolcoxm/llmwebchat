# Use a flat (npm-style) node_modules layout. This avoids pnpm's symlink/rename
# bookkeeping (e.g. `typescript -> .ignored_typescript`) which fails with EPERM
# on Windows when files are locked by OneDrive / Defender / an editor's TS server.
node-linker=hoisted
